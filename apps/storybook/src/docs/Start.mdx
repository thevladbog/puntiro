import { Meta } from '@storybook/addon-docs/blocks';
import { LocalizedArticle } from './LocalizedArticle';
import { StartLayout } from './StartLayout';
import { Button } from '@puntiro/ui';

<Meta title="Start" />

<LocalizedArticle id="start"><StartLayout preview={(locale) => <Button iconBefore="printer">{locale === 'ru' ? 'Напечатать' : 'Print'}</Button>} /></LocalizedArticle>
